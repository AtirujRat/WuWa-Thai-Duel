import {updateBattleLog} from './battle-log.js';
import {formatCardText} from './card-text.js';
import {phaseTrack} from './phase-track.js';
import {rulesBoard,setupRulesUI,showRuleCard,opponentHand} from './rules-ui.js';
import {installSound,pickup,release,updateEffects} from './card-fx.js';
installSound();
import {validateDeck,presetDeck} from './deck-rules.js';
let placeFaceDown=false,tableDrag=null,suppressTableClick=false;
function playmat(p,mine,live){
 const legacy=z=>(p[z]||[]).map((code,i)=>`<button class="mat-card ${z==='concerto'?'horizontal':''}" style="left:8px;top:${8+i*28}px" ${mine&&live?`data-zone="${z}" data-index="${i}"`:`data-detail="${code}"`} aria-label="${esc(card(code).name)}"><img src="${card(code).img}" alt="${esc(card(code).name)}"></button>`).join('');
 const zone=z=>`<div class="mat-zone mat-${z}" ${mine&&live&&(!room.rulesVersion||(z==='concerto'?room.phase==='action'&&!p.used?.charge:['battle','defense'].includes(room.phase)))?`data-drop-zone="${z}"`:''}><span class="mat-label">${z==='concerto'?'คอนแชร์โต · แนวนอน':'สนามแอ็กชัน'}</span>${legacy(z)}${(p.table||[]).filter(c=>c.zone===z).map(c=>`<button data-fx-card="${c.id}" class="mat-card ${z==='concerto'?'horizontal':''} ${c.faceDown?'face-down':''}" style="left:${c.x*100}%;top:${c.y*100}%;transform:translate(-${c.x*100}%,-${c.y*100}%)" ${mine&&live?`data-table-drag="${c.id}" data-table-menu="${c.id}"`:!c.faceDown?`data-detail="${c.code}"`:''} aria-label="${c.faceDown?'การ์ดคว่ำ':esc(card(c.code).name)}">${c.faceDown?'<span class="mat-back" aria-hidden="true"></span>':`<img draggable="false" src="${card(c.code).img}" alt="${esc(card(c.code).name)}">`}</button>`).join('')}</div>`;
 const field=[...p.field.filter(code=>code!==p.leader).slice(0,1),p.leader,...p.field.filter(code=>code!==p.leader).slice(1)].filter(Boolean);
 return `<div class="playmat ${mine?'own-mat':'opponent-mat'}">${zone('concerto')}${zone('action')}<div class="mat-characters">${field.map(code=>`<div class="character-slot ${code===p.leader?'leader-slot':''}"><button data-detail="${code}" class="art-button">${img(card(code))}</button><span>${code===p.leader?'ผู้นำ':'แบ็ค'} · Lv.${card(code).level}</span>${mine&&(room.rulesVersion?(room.status==='waiting'||room.phase==='action'&&room.active===room.seat&&!room.choice&&!p.used?.switch):live&&room.mode!=='bot')&&code!==p.leader?btn('leader','เป็นผู้นำ',`data-code="${code}"`):''}</div>`).join('')}</div><div class="character-deck"><span class="deck-back pale" role="img" aria-label="หลังการ์ดตัวละคร"></span><span>เด็คตัวละคร · ${p.reserveCount}</span>${mine?(room.rulesVersion?'<button data-rule="reserve">ดู / อัปเลเวล</button>':btn('showReserve','ดูการ์ด')):''}</div><div class="mat-trash">${zoneUI(p,'trash',mine,live&&!room.rulesVersion&&room.mode!=='bot')}</div><div class="mat-deck"><span class="deck-back" role="img" aria-label="หลังการ์ดแอ็กชัน"></span><span>สำรับแอ็กชัน · ${p.deckCount}</span>${mine&&!room.rulesVersion?`<div class="deck-buttons">${btn('draw','จั่ว',live&&p.deckCount?'':'disabled','primary')}${btn('shuffle','สับเด็ค',live&&p.deckCount>1?'':'disabled')}</div>`:''}</div></div>`;

}
document.addEventListener('change',e=>{if(e.target.name==='placement')placeFaceDown=e.target.value==='down'});
document.addEventListener('click',async e=>{
 if(suppressTableClick){e.preventDefault();e.stopImmediatePropagation();return}
 const menu=e.target.closest('[data-table-menu]'),place=e.target.closest('[data-place-hand]'),command=e.target.closest('[data-table-command]');
 if(menu&&room.rulesVersion){e.stopImmediatePropagation();showRuleCard(menu.dataset.tableMenu);return}if(menu){e.stopImmediatePropagation();const c=room.players[room.seat].table.find(c=>c.id===menu.dataset.tableMenu);detail(c.code,`<p>${c.faceDown?'การ์ดนี้คว่ำอยู่ คู่เล่นมองไม่เห็นหน้าการ์ด':'การ์ดนี้หงายอยู่'}</p><div class="row"><button data-table-command="tableFlip" data-id="${c.id}">${c.faceDown?'หงาย':'คว่ำ'}การ์ด</button><button data-table-command="tableTake" data-id="${c.id}" data-to="hand">กลับมือ</button>${room.mode==='bot'?'':`<button data-table-command="tableTake" data-id="${c.id}" data-to="trash">กองทิ้ง</button>`}</div>`)}
 if(place&&room.rulesVersion){e.stopImmediatePropagation();showRuleCard(null,Number(place.dataset.placeHand));return}if(place){pickup(null);e.stopImmediatePropagation();const i=Number(place.dataset.placeHand);dialog.innerHTML=`<h2>วาง ${esc(card(room.players[room.seat].hand[i]).name)}</h2><p>ลงแบบ${placeFaceDown?'คว่ำ':'หงาย'} · เปลี่ยนได้ที่ตัวเลือกเหนือสนาม</p><div class="row">${['action','concerto'].map(z=>`<button data-table-command="tablePlace" data-index="${i}" data-zone="${z}">${z==='action'?'สนามแอ็กชัน':'ช่องซ้ายแนวนอน'}</button>`).join('')}${btn('close','ปิด')}</div>`;dialog.showModal()}
 if(command){e.stopImmediatePropagation();const d=command.dataset;dialog.close();await action({type:d.tableCommand,id:d.id,to:d.to,index:Number(d.index),code:room.players[room.seat].hand[Number(d.index)],zone:d.zone,x:.5,y:.5,faceDown:placeFaceDown,version:room.version})}
},true);
document.addEventListener('dragstart',e=>{if(e.target.closest('[data-hand-drag],[data-table-drag]'))e.preventDefault()});
document.addEventListener('pointerdown',e=>{
 const el=e.target.closest('[data-hand-drag],[data-table-drag]');if(!el||e.button!==0||busy||e.target.closest('button:not(.art-button):not(.mat-card)'))return;
 tableDrag={el,startX:e.clientX,startY:e.clientY,pointer:e.pointerId,version:room.version,id:el.dataset.tableDrag,index:Number(el.dataset.handDrag),code:el.dataset.code,ghost:null};
});
document.addEventListener('pointermove',e=>{
 const d=tableDrag;if(!d||e.pointerId!==d.pointer)return;
 if(!d.ghost&&Math.hypot(e.clientX-d.startX,e.clientY-d.startY)<7)return;
 e.preventDefault();if(!d.ghost){pickup(d.el);d.ghost=d.el.cloneNode(true);d.ghost.className='table-drag-ghost';d.ghost.removeAttribute('style');document.body.append(d.ghost);document.body.classList.add('table-dragging')}
 d.ghost.style.left=e.clientX+'px';d.ghost.style.top=e.clientY+'px';document.querySelectorAll('.drop-hover').forEach(el=>el.classList.remove('drop-hover'));document.elementFromPoint(e.clientX,e.clientY)?.closest('[data-drop-zone]')?.classList.add('drop-hover');
},{passive:false});
async function finishTableDrag(e,cancel=false){const d=tableDrag;if(!d||e.pointerId!==d.pointer)return;tableDrag=null;release(d.el);document.body.classList.remove('table-dragging');document.querySelectorAll('.drop-hover').forEach(el=>el.classList.remove('drop-hover'));if(!d.ghost)return;d.ghost.remove();suppressTableClick=true;setTimeout(()=>suppressTableClick=false,150);if(cancel)return;const target=document.elementFromPoint(e.clientX,e.clientY)?.closest('[data-drop-zone]');if(!target){toast('ลากไปวางในสนามของคุณหรือช่องซ้าย');return}const r=target.getBoundingClientRect();await action({type:d.id?'tableMove':'tablePlace',id:d.id,index:d.index,code:d.code,zone:target.dataset.dropZone,x:Math.max(0,Math.min(1,(e.clientX-r.left)/r.width)),y:Math.max(0,Math.min(1,(e.clientY-r.top)/r.height)),faceDown:placeFaceDown,version:d.version})}
document.addEventListener('pointerup',e=>finishTableDrag(e));
document.addEventListener('pointercancel',e=>finishTableDrag(e,true));
const $=s=>document.querySelector(s),esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
let cards=[],meta={},saved=[],tab='cards',query='',set='',kind='',character='',page=1,deck={name:'เด็คของฉัน',entries:{}},room=null,token='',busy=false,selectedMulligan=new Set(),syncError=false;
const app=$('#app'),dialog=$('#detail');let toastTimer;

function toast(s){$('#toast').textContent=s;$('#toast').style.display='block';clearTimeout(toastTimer);toastTimer=setTimeout(()=>$('#toast').style.display='none',4000)}
async function api(url,data,auth=''){const r=await fetch(url,{method:data===undefined?'GET':'POST',headers:{'Content-Type':'application/json',...(auth?{Authorization:'Bearer '+auth}:{})},...(data===undefined?{}:{body:JSON.stringify(data)})});const j=await r.json();if(!r.ok)throw Error(j.error||'ติดต่อเซิร์ฟเวอร์ไม่ได้');return j}
const card=code=>cards.find(c=>c.code===code);
setupRulesUI({getRoom:()=>room,send:action,dialog,card,esc,showCard:detail});
const btn=(action,label,extra='',cls='')=>`<button class="${cls}" data-do="${action}" ${extra}>${label}</button>`;
const img=c=>`<img class="art" src="${esc(c.img)}" alt="${esc(c.name)} ${c.code}" loading="lazy">`;
function tile(c,add=true){return `<article class="card"><button class="art-button" data-detail="${c.code}" aria-label="อ่าน ${esc(c.name)}">${img(c)}</button><div class="card-head"><span class="code">${c.code}</span>${add?btn('add','+',`data-code="${c.code}" aria-label="เพิ่ม ${esc(c.name)} ลงเด็ค"`,'add'):''}</div><h3>${esc(c.name)}</h3><div class="card-meta">${c.type==='character'?'ตัวละคร · Lv. '+c.level:c.color+' · ค่าใช้ '+c.fee+' · พลัง '+c.damage}</div></article>`}
function filters(){return `<div class="toolbar"><input class="search" id="search" aria-label="ค้นหาการ์ด" placeholder="ค้นหาชื่อ ความสามารถ หรือรหัสการ์ด…" value="${esc(query)}"><select id="set" aria-label="ชุดการ์ด"><option value="">ทุกชุด</option>${['SD01','SD02','BP01'].map(s=>`<option ${set===s?'selected':''}>${s}</option>`).join('')}</select><select id="kind" aria-label="ประเภท"><option value="">ทุกประเภท</option><option value="character" ${kind==='character'?'selected':''}>ตัวละคร</option><option value="action" ${kind==='action'?'selected':''}>แอ็กชัน</option></select><select id="character" aria-label="ตัวละคร"><option value="">ทุกตัวละคร</option>${[...new Set(cards.map(c=>c.character))].map(s=>`<option ${character===s?'selected':''}>${esc(s)}</option>`).join('')}</select></div>`}
function filtered(){return cards.filter(c=>(!set||c.set===set)&&(!kind||c.type===kind)&&(!character||c.character===character)&&[c.name,c.nameTh,c.nameJp,c.nameEn,c.code,c.effectTh,...c.tags].join(' ').toLowerCase().includes(query.toLowerCase()))}
function results(){const arr=filtered();const pages=Math.max(1,Math.ceil(arr.length/24));page=Math.min(page,pages);return `<p class="muted small">${arr.length} รหัสการ์ด · กดภาพเพื่ออ่านคำแปล</p><div class="grid">${arr.slice((page-1)*24,page*24).map(c=>tile(c)).join('')||'<div class="empty">ไม่พบการ์ดที่ตรงกับการค้นหา</div>'}</div><div class="pagination">${btn('prev','← ก่อนหน้า',page===1?'disabled':'')}<span>${page} / ${pages}</span>${btn('next','ถัดไป →',page===pages?'disabled':'')}</div>`}
function deckPanel(){const v=validateDeck(deck.entries,cards);return `<aside class="panel"><h2>เด็คของคุณ</h2><input id="deckName" aria-label="ชื่อเด็ค" maxlength="80" value="${esc(deck.name)}" style="width:100%"><div class="row" style="margin:16px 0"><div><span class="count">${v.actions}</span><span class="muted"> / 40 แอ็กชัน</span></div><div><span class="count">${v.characters}</span><span class="muted"> ตัวละคร</span></div></div><div class="small ${v.valid?'pill':'muted'}">${v.valid?'✓ ผ่านการตรวจเด็คเบื้องต้น':v.errors.slice(0,3).map(esc).join('<br>')}</div><div class="row" style="margin:16px 0">${btn('save','บันทึกเด็ค','','primary')}${btn('export','ส่งออก')}${btn('new','เด็คใหม่')}</div>${Object.entries(deck.entries).map(([code,n])=>{const c=card(code);return `<div class="deck-row"><button class="art-button" style="width:38px" data-detail="${code}"><img src="${c.img}" alt="${esc(c.name)}"></button><div class="grow">${esc(c.name)}<div class="code">${code}</div></div>${btn('remove','−',`data-code="${code}" aria-label="ลด ${esc(c.name)}"`,'add')}<span>${n}</span>${btn('add','+',`data-code="${code}" aria-label="เพิ่ม ${esc(c.name)}"`,'add')}</div>`}).join('')||'<p class="muted small">กด + ที่การ์ดเพื่อเพิ่ม หรือเลือกเด็คฝึกเล่น</p>'}</aside>`}
function catalog(){return `<div class="stats"><div><strong>${cards.length}</strong> รหัสการ์ด</div><div><strong>${meta.records}</strong> ภาพการ์ด</div><div><strong>TH</strong> คำแปลฉบับร่าง</div></div><p class="small muted">ข้อมูล UCP ${meta.date} · ครบรายการที่ฐานข้อมูลญี่ปุ่นส่งกลับมา · <a href="#" data-do="sources">ขอบเขตข้อมูล</a></p>${filters()}<div class="split"><section id="results">${results()}</section><div id="deckPanel">${deckPanel()}</div></div>`}
function decksPage(){return `<div class="row subnav">${btn('preset1','ใช้เด็คฝึก SD01')}${btn('preset2','ใช้เด็คฝึก SD02')}${btn('import','นำเข้าเด็ค')}<input id="importFile" type="file" accept="application/json,.json" hidden></div><p class="muted small">เด็คฝึกเป็นรายการที่จัดให้ลองเล่น ไม่ใช่จำนวนการ์ดตามกล่องจริง · เด็คบันทึกไว้ในเซิร์ฟเวอร์เดโมบนเครื่องนี้</p><div class="split"><section><h2>เด็คที่บันทึก <span class="badge">${saved.length}</span></h2><div class="stack">${saved.map(d=>`<div class="panel"><h3>${esc(d.name)}</h3><p class="muted small">${validateDeck(d.entries,cards).actions} แอ็กชัน · ${validateDeck(d.entries,cards).characters} ตัวละคร</p><div class="row">${btn('load','เปิดเด็ค',`data-id="${d.id}"`)}${btn('use','ใช้เล่น',`data-id="${d.id}"`,'primary')}</div></div>`).join('')||'<div class="empty">ยังไม่มีเด็คที่บันทึก<br>เริ่มจากเด็คฝึกหรือเลือกการ์ดจากคลัง</div>'}</div><div class="panel" style="margin-top:24px"><h3>ตรวจเด็คเบื้องต้น</h3><p class="muted small">แอ็กชัน 40 ใบ รหัสละไม่เกิน 3 ใบ · ตัวละคร 3–15 ใบ รหัสละ 1 ใบ พร้อมตัวละครเลเวล 0 จำนวน 3 ตัว และแอ็กชันที่ตรงกับทีม</p><a class="small" href="https://w.atwiki.jp/wuwatcgwiki/pages/129.html" target="_blank" rel="noreferrer">อ้างอิงกฎจัดเด็ค ↗</a></div></section><div id="deckPanel">${deckPanel()}</div></div>`}
function playLobby(){const valid=validateDeck(deck.entries,cards);return `<div class="notice">ห้องใหม่ใช้กฎตามเฟส: จั่วอัตโนมัติ ชาร์จ อัปเลเวล เปิดแอ็กชันพร้อมกัน และคอมโบ · เอฟเฟกต์ชุดเสริมบางใบใช้แผงจัดการด้วยตนเอง</div><div class="split"><div class="panel"><h2>เริ่มดวล 1 ต่อ 1</h2><div class="stack"><label>ชื่อของคุณ<input id="playerName" maxlength="32" placeholder="ชื่อที่ใช้ในห้อง" style="display:block;width:100%"></label><p>เด็คที่เลือก: <strong>${esc(deck.name)}</strong><br><span class="small ${valid.valid?'pill':'danger'}">${valid.valid?'พร้อมเล่น · '+valid.actions+' แอ็กชัน':esc(valid.errors.join(' · '))}</span></p><div class="row">${btn('preset1','ใช้เด็คฝึก SD01')}${btn('preset2','ใช้เด็คฝึก SD02')}</div><div class="bot-lobby"><h3>เล่นกับบอท</h3><p class="small muted">กฎพื้นฐานตามคู่มือ 12 ก.ย. 2026 · เด็คเริ่มต้นประมวลผลเอฟเฟกต์อัตโนมัติ · ชุดเสริมบางใบต้องจัดการเอฟเฟกต์ผ่านแผงยืนยัน</p><div class="row"><label>ระดับบอท <select id="botDifficulty"><option value="easy">ง่าย · สุ่มการ์ด</option><option value="normal" selected>ปกติ · เลือกตามพลัง</option></select></label><label>เด็คบอท <select id="botDeck"><option>SD02</option><option>SD01</option></select></label></div><div style="margin-top:16px">${btn('startBot','เริ่มเล่นกับบอท',valid.valid?'':'disabled','primary')}</div></div><h3>เล่นกับเพื่อน</h3>${btn('create','สร้างห้อง',valid.valid?'':'disabled')}<hr style="width:100%;border:0;border-top:1px solid var(--line)"><label>รหัสห้องของเพื่อน<input id="roomCode" maxlength="8" value="${esc(new URLSearchParams(location.search).get('room')||'')}" placeholder="รหัส 8 ตัว (แยกตัวพิมพ์เล็ก/ใหญ่)" style="display:block;width:100%"></label>${btn('join','เข้าห้องเพื่อน',valid.valid?'':'disabled')}</div></div><aside class="panel"><h3>ลองสองคนได้อย่างไร</h3><p class="muted">เครื่องเดียว: เปิดเว็บในอีกเบราว์เซอร์ แล้วใส่รหัสห้อง</p><p class="muted">คนละเครื่อง: อยู่ Wi-Fi เดียวกัน เปิดที่อยู่ LAN ที่ปรากฏตอนเริ่มเดโม แล้วใส่รหัสห้องเดียวกัน</p><p class="small muted">การเล่นผ่านอินเทอร์เน็ตคนละเครือข่ายจะเพิ่มเมื่อโฮสต์จริง เก็บหน้าต่างเซิร์ฟเวอร์ไว้ระหว่างเล่น</p>${room?btn('resume','กลับเข้าห้องเดิม'):''}</aside></div>`}
const zoneNames={hand:'มือ',deck:'ใต้เด็ค',action:'แอ็กชัน',concerto:'คอนแชร์โต',trash:'กองทิ้ง'};
function fieldUI(p,mine,live){return `<div class="row">${p.field.map(code=>{const c=card(code);return `<div style="text-align:center"><button class="art-button" style="width:80px" data-detail="${code}">${img(c)}</button><div class="small">${esc(c.name)} Lv.${c.level}</div>${p.leader===code?'<span class="badge pill">ผู้นำ</span>':mine&&live?btn('leader','เป็นผู้นำ',`data-code="${code}"`):''}</div>`}).join('')}</div>`}
function zoneUI(p,zone,mine,live){return `<div class="zone"><h3>${zoneNames[zone]} <span class="badge">${p[zone].length}</span></h3><div class="row">${p[zone].map((code,i)=>`<button class="art-button" style="width:62px" ${mine&&live?`data-zone="${zone}" data-index="${i}"`:`data-detail="${code}"`} aria-label="${esc(card(code).name)}"><img class="mini" style="width:100%" src="${card(code).img}" alt="${esc(card(code).name)}"></button>`).join('')}</div></div>`}
function board(){if(room.rulesVersion)return rulesBoard(room,{card,esc,img,playmat,btn,selectedMulligan});
 const me=room.players[room.seat],op=room.players[1-room.seat],bot=room.mode==='bot',waiting=room.status==='waiting',live=room.status==='playing',canPlace=live&&(!bot||room.botPhase==='choose');
 return `<section class="battle-shell"><div class="battle-bar"><strong>${bot?'ฝึกกับบอท':'ห้อง '+room.code}</strong><span>${syncError?'กำลังเชื่อมต่อใหม่…':op?esc(op.name)+' · มือ '+op.handCount+' · ไลฟ์ '+op.hp:'รอเพื่อนเข้าห้อง'}</span><div>${!bot?btn('copy','รหัสห้อง'):''}${btn('battleInfo','บันทึก / กติกา')}${btn('lobby','ออกจากสนาม')}</div></div><div class="battle-half opponent-half">${opponentHand(op)}${op?playmat(op,false,false):'<div class="waiting-seat">รอเพื่อนเข้าห้อง · '+room.code+'</div>'}</div><div class="battle-divider">${phaseTrack(room)}<strong>${waiting?'เตรียมตัว':room.status==='finished'?(bot?(room.winner===null?'เสมอ':room.winner===0?'คุณชนะ':'บอทชนะ'):'จบเกม'):bot?room.turn+' / 40 รอบ':'เทิร์น '+room.turn}</strong><span>${esc(me.name)} · ไลฟ์ ${me.hp}${bot?' · พลังงาน '+me.energy+'/3':''}</span><div class="battle-actions">${waiting?`${btn('mulligan','เปลี่ยนไพ่ที่เลือก',me.ready||me.mulligan?'disabled':'')}${btn('ready',me.ready?'พร้อมแล้ว':'พร้อมเล่น',me.ready?'disabled':'','primary')}`:live?bot?(room.botPhase==='result'?btn('nextRound','รอบถัดไป →','','primary'):`${btn('resolveTable','เปิดการ์ดและดวล',!(me.table||[]).some(c=>c.zone==='action')?'disabled':'','primary')}${btn('pass','ผ่าน',(me.table||[]).some(c=>c.zone==='action')?'disabled':'')}`):`${btn('hpDown','− ไลฟ์')}${btn('hpUp','+ ไลฟ์')}${btn('end','ส่งเทิร์น',room.active===room.seat?'':'disabled')}${btn('clear','เก็บแอ็กชัน')}`:''}${live?btn('surrender','ยอมแพ้'):''}</div>${canPlace?`<div class="placement"><label><input type="radio" name="placement" value="up" ${placeFaceDown?'':'checked'}> หงาย</label><label><input type="radio" name="placement" value="down" ${placeFaceDown?'checked':''}> คว่ำ</label></div>`:''}${bot&&room.lastDuel?`<span class="round-result">${esc(room.lastDuel.reason)} · เสียหาย ${room.lastDuel.damage}</span>`:''}</div><div class="battle-half">${playmat(me,true,canPlace)}</div><div class="hand-dock"><span class="hand-count">มือ ${me.handCount}</span><div class="hand">${me.hand.map((code,i)=>`<article class="card" ${canPlace?`data-hand-drag="${i}" data-code="${code}"`:''}><button class="art-button" data-detail="${code}" aria-label="${esc(card(code).name)}">${img(card(code))}</button>${canPlace?`<button data-place-hand="${i}" class="quick-place">วาง</button>`:waiting&&!me.ready&&!me.mulligan?`<label><input type="checkbox" data-mulligan="${i}" ${selectedMulligan.has(i)?'checked':''}> เปลี่ยน</label>`:''}</article>`).join('')}</div></div></section>`;
}

let inLobby=false;
function render(){document.body.classList.toggle('battle-view',tab==='play'&&!!room&&!inLobby);document.querySelectorAll('[data-tab]').forEach(n=>n.classList.toggle('active',n.dataset.tab===tab));$('.heading h1').textContent=tab==='cards'?'ทุกการ์ด เริ่มต้นการดวลของคุณ':tab==='decks'?'สร้างทีม จัดเด็ค พร้อมดวล':'เลือกคู่ดวลของคุณ';$('.heading .muted').textContent=tab==='cards'?'ค้นหาการ์ดจริง อ่านคำแปลไทย และสร้างเด็คในแบบของคุณ':tab==='decks'?'เลือกตัวละครที่เข้ากัน แล้วเติมแอ็กชันให้ครบ 40 ใบ':'ฝึกกับบอทคนเดียว หรือเปิดโต๊ะเล่นกับเพื่อน';app.innerHTML=tab==='cards'?catalog():tab==='decks'?decksPage():room&&!inLobby?board():playLobby();updateEffects(room,tab==='play'&&!inLobby);updateBattleLog(room,tab==='play'&&!inLobby);}
function refreshDeck(){const el=$('#deckPanel');if(el)el.innerHTML=deckPanel()}
function detail(code,extra=""){const c=card(code);if(!c)return;if(dialog.open)dialog.close();const panel=$("#card-info");panel.hidden=false;document.body.classList.add("has-card-info");panel.innerHTML=`<div class="card-info-heading"><strong>รายละเอียดการ์ด</strong><button data-do="closeCardInfo" aria-label="ปิดรายละเอียดการ์ด">✕</button></div><div class="card-info-content"><div class="detail-grid"><img id="detailArt" src="${c.img}" alt="${esc(c.name)}"><div><p class="eyebrow">${c.code} · ${c.set}</p><h2>${esc(c.name)}</h2>${extra}<p class="muted">${esc(c.nameJp)}<br>${esc(c.nameEn)}</p><div class="row">${c.type==='character'?`<span class="badge">Lv. ${c.level}</span><span class="badge">${esc(c.element)}</span><span class="badge">${esc(c.weapon)}</span>`:`<span class="badge">${c.color}</span><span class="badge">ค่าใช้ ${c.fee}</span><span class="badge">ความเร็ว ${c.speed||'—'}</span><span class="badge">ความเสียหาย ${c.damage||'0'}</span>`}</div><p class="small muted">${c.tags.map(esc).join(' · ')}</p><div class="effect">${formatCardText(c.effectTh)}</div><p class="small muted">คำแปลไทยไม่เป็นทางการ · ฉบับร่าง</p><details><summary>ข้อความญี่ปุ่นต้นฉบับ</summary><p class="effect">${formatCardText(c.info||'—')}</p></details><label class="small">ภาพเวอร์ชัน <select id="variant">${c.variants.map((v,i)=>`<option value="${v.img}">${v.rarity} · ${esc(v.obtain)} · ${i+1}</option>`).join('')}</select></label><div class="row" style="margin-top:20px">${btn('add','+ เพิ่มลงเด็ค',`data-code="${code}"`,'primary')}<a href="${c.source}" target="_blank" rel="noreferrer">ข้อมูลต้นฉบับ ↗</a></div></div></div></div>`;panel.querySelector(".card-info-content").scrollTop=0}
function moveDialog(zone,index){const code=room.players[room.seat][zone][index];detail(code,`<p class="muted">จาก ${zoneNames[zone]}</p><div class="row">${Object.entries(zoneNames).filter(([z])=>z!==zone).map(([z,n])=>btn('move',n,`data-from="${zone}" data-to="${z}" data-index="${index}"`)).join('')}</div>`)}
async function action(cmd){if(busy)return;busy=true;if((room.mode==='bot'||room.rulesVersion)&&cmd.version===undefined)cmd.version=room.version;try{room=await api('/api/rooms/'+room.code,cmd,token);syncError=false;render()}catch(e){toast(e.message)}finally{busy=false}}
function add(code,delta){const c=card(code),n=(deck.entries[code]||0)+delta;if(n>(c.type==='character'?1:3))return toast('รหัสนี้ใส่ได้สูงสุด '+(c.type==='character'?1:3)+' ใบ');if(n<=0)delete deck.entries[code];else deck.entries[code]=n;refreshDeck();if(delta>0)toast('เพิ่ม '+c.name+' แล้ว')}
function exportDeck(){const blob=new Blob([JSON.stringify({format:'wuwa-thai-deck-v1',name:deck.name,entries:deck.entries},null,2)],{type:'application/json'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download='wuwa-deck.json';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000)}
document.addEventListener('click',async e=>{const t=e.target.closest('button,a');if(!t)return;try{
 if(t.dataset.tab){tab=t.dataset.tab;render();return}
 if(t.dataset.detail){detail(t.dataset.detail);return}
 if(t.dataset.zone){moveDialog(t.dataset.zone,Number(t.dataset.index));return}
 const doIt=t.dataset.do;if(!doIt)return;e.preventDefault();
 if(doIt==='battleInfo'){dialog.innerHTML=btn('close','ปิด','','close')+'<h2>ข้อมูลการดวล</h2><p>'+(room.mode==='bot'?'โหมดฝึกกติกาย่อ · แดงชนะเขียว เขียวชนะน้ำเงิน น้ำเงินชนะแดง · สีเดียวกันตัดสินด้วยความเร็ว · จั่วเองเพิ่มได้ในช่วงเลือกการ์ด · ช่องซ้ายไม่มีผลเพิ่มพลัง':'โต๊ะจำลอง · ผู้เล่นจัดการเอฟเฟกต์และค่าใช้เอง')+'</p>'+room.log.slice().reverse().map(x=>'<p>'+esc(x.text)+'</p>').join('');dialog.showModal();return}
 if(doIt==='showReserve'){const p=room.players[room.seat];dialog.innerHTML=btn('close','ปิด','','close')+'<h2>เด็คตัวละคร</h2><div class="row">'+p.reserve.map(code=>'<div>'+tile(card(code),false)+(room.mode!=='bot'&&room.status==='playing'?btn('level','เปลี่ยนเลเวล', 'data-code="'+code+'"'):'')+'</div>').join('')+'</div>';dialog.showModal();return}
 if(doIt==='closeCardInfo'){$('#card-info').hidden=true;document.body.classList.remove('has-card-info');return}
 if(doIt==='close'){dialog.close();return}
 if(doIt==='add'||doIt==='remove'){add(t.dataset.code,doIt==='add'?1:-1);return}
 if(doIt==='prev'||doIt==='next'){page+=doIt==='next'?1:-1;$('#results').innerHTML=results();$('#results').scrollIntoView({behavior:'smooth',block:'start'});return}
 if(doIt==='preset1'||doIt==='preset2'){deck=presetDeck(doIt==='preset1'?'SD01':'SD02',cards);render();toast('โหลดเด็คฝึกแล้ว');return}
 if(doIt==='new'){if(Object.keys(deck.entries).length&&!confirm('เริ่มเด็คใหม่? การแก้ไขที่ยังไม่บันทึกจะหายไป'))return;deck={name:'เด็คของฉัน',entries:{}};render();return}
 if(doIt==='save'){deck=await api('/api/decks',deck);saved=await api('/api/decks');if(tab==='decks')render();toast('บันทึกเด็คแล้ว');return}
 if(doIt==='load'||doIt==='use'){deck=structuredClone(saved.find(d=>d.id===t.dataset.id));if(doIt==='use'){tab='play';inLobby=true}render();return}
 if(doIt==='export'){exportDeck();return}
 if(doIt==='import'){$('#importFile').click();return}
 if(doIt==='sources'){dialog.innerHTML=`${btn('close','✕','','close')}<h2>ขอบเขตข้อมูล</h2><p>รวบรวมจาก UCP วันที่ ${meta.date}: ${meta.records} รายการภาพ / ${cards.length} รหัสการ์ด</p><p>SD01 23 รหัส · SD02 23 รหัส · BP01 77 รหัส</p><p>${esc(meta.scope)}</p><p>${esc(meta.translation)}</p><p>เลขรหัสซ้ำรวมเป็นการ์ดเดียวสำหรับจัดเด็ค เลือกภาพลายอื่นได้ในรายละเอียด</p><a href="${meta.source}" target="_blank" rel="noreferrer">เว็บไซต์ทางการ ↗</a>`;dialog.showModal();return}
 if(doIt==='startBot'){
  const name=$('#playerName').value.trim()||'คุณ';t.disabled=true;
  try{const result=await api('/api/rooms/bot',{name,entries:deck.entries,difficulty:$('#botDifficulty').value,botDeck:$('#botDeck').value});room=result.room;token=result.token;selectedMulligan.clear();sessionStorage.setItem('wuwa-room',JSON.stringify({code:room.code,token}));inLobby=false;render()}finally{t.disabled=false}return;
 }
 if(doIt==='nextRound'||doIt==='pass'||doIt==='resolveTable'){await action({type:doIt});return}
 if(doIt==='create'||doIt==='join'){
const name=$('#playerName').value.trim();if(!name)return toast('กรอกชื่อผู้เล่นก่อน');t.disabled=true;try{const result=await api('/api/rooms/'+doIt,{name,entries:deck.entries,...(doIt==='join'?{code:$('#roomCode').value.trim()}:{})});room=result.room;token=result.token;sessionStorage.setItem('wuwa-room',JSON.stringify({code:room.code,token}));inLobby=false;render()}finally{t.disabled=false}return}
 if(doIt==='copy'){try{await navigator.clipboard.writeText(room.code);toast('คัดลอกรหัสห้องแล้ว')}catch{toast('รหัสห้อง: '+room.code)}return}
 if(doIt==='lobby'){inLobby=true;render();return}
 if(doIt==='resume'){inLobby=false;render();return}
 if(doIt==='reserve'){const p=room.players[room.seat];dialog.innerHTML=`${btn('close','✕','','close')}<h2>เลือกเลเวลตัวละคร</h2><p class="muted">เปลี่ยนเมื่อกฎหรือเอฟเฟกต์อนุญาต ระบบไม่คิดค่าใช้ให้</p><div class="grid">${p.reserve.map(code=>`<div>${tile(card(code),false)}${btn('level','ใช้เลเวลนี้',`data-code="${code}"`)}</div>`).join('')}</div>`;dialog.showModal();return}
 if(doIt==='surrender'&&!confirm('ยืนยันยอมแพ้และจบเกมนี้?'))return;
 if(doIt==='move'){await action({type:'move',from:t.dataset.from,to:t.dataset.to,index:Number(t.dataset.index)});dialog.close();return}
 if(doIt==='level'){await action({type:'level',code:t.dataset.code});dialog.close();return}
 const commands={draw:{type:'draw'},shuffle:{type:'shuffle'},hpUp:{type:'hp',delta:1},hpDown:{type:'hp',delta:-1},ready:{type:'ready'},mulligan:{type:'mulligan',indices:[...selectedMulligan]},commit:{type:'commit',index:Number(t.dataset.index)},cancel:{type:'cancel'},leader:{type:'leader',code:t.dataset.code},reveal:{type:'reveal'},end:{type:'end'},clear:{type:'clear'},surrender:{type:'surrender'},topConcerto:{type:'move',from:'deck',to:'concerto',index:0},topTrash:{type:'move',from:'deck',to:'trash',index:0}};
 if(commands[doIt]){await action(commands[doIt]);if(doIt==='mulligan')selectedMulligan.clear()}
 }catch(err){toast(err.message)}});
document.addEventListener('input',e=>{if(e.target.id==='search'){query=e.target.value;page=1;$('#results').innerHTML=results()}if(e.target.id==='deckName')deck.name=e.target.value});
document.addEventListener('change',async e=>{const t=e.target;if(['set','kind','character'].includes(t.id)){if(t.id==='set')set=t.value;if(t.id==='kind')kind=t.value;if(t.id==='character')character=t.value;page=1;$('#results').innerHTML=results()}if(t.id==='variant')$('#detailArt').src=t.value;if(t.dataset.mulligan!==undefined){const i=Number(t.dataset.mulligan);if(t.checked)selectedMulligan.add(i);else selectedMulligan.delete(i)}if(t.id==='importFile'){try{if(t.files[0].size>50000)throw Error('ไฟล์ใหญ่เกินไป');const d=JSON.parse(await t.files[0].text());if(typeof d.name!=='string'||d.name.length>80||!d.entries||typeof d.entries!=='object'||Array.isArray(d.entries))throw Error('รูปแบบเด็คไม่ถูกต้อง');for(const [code,n] of Object.entries(d.entries)){const c=card(code);if(!c||!Number.isInteger(n)||n<1||n>(c.type==='character'?1:3))throw Error('รหัสหรือจำนวนการ์ดไม่ถูกต้อง')}deck={name:d.name,entries:d.entries};render();toast('นำเข้าเด็คแล้ว กดบันทึกเพื่อเก็บไว้')}catch(err){toast(err.message)}}});
try{[cards,meta,saved]=await Promise.all([api('/cards.json'),api('/catalog-meta.json'),api('/api/decks')]);const last=sessionStorage.getItem('wuwa-room');if(last){try{const s=JSON.parse(last);token=s.token;room=await api('/api/rooms/'+s.code,undefined,token)}catch{sessionStorage.removeItem('wuwa-room')}}if(new URLSearchParams(location.search).has('room'))tab='play';render()}catch(e){app.innerHTML=`<div class="notice">โหลดเดโมไม่สำเร็จ: ${esc(e.message)} · เปิดผ่าน START-DEMO.cmd แล้วลองใหม่</div>`}
setInterval(async()=>{if(!room||busy||tableDrag)return;try{const next=await api('/api/rooms/'+room.code,undefined,token);if(tableDrag)return;const changed=next.version!==room.version||syncError;room=next;syncError=false;if(changed&&tab==='play'&&!inLobby)render()}catch{if(!syncError){syncError=true;if(tab==='play'&&!inLobby)render()}}},1000);
if(document.modelContext?.registerTool){try{document.modelContext.registerTool({name:'search_wuwa_cards',description:'Search the loaded WuWa card catalog and display matching cards.',inputSchema:{type:'object',properties:{query:{type:'string'}},required:['query'],additionalProperties:false},annotations:{readOnlyHint:true},execute(input){if(typeof input?.query!=='string')throw Error('query must be a string');query=input.query;set='';kind='';character='';page=1;tab='cards';render();return filtered().map(c=>({code:c.code,name:c.name,effect:c.effectTh}))}})}catch{}}

document.addEventListener("keydown",e=>{if(e.key==="Escape"&&!dialog.open){$("#card-info").hidden=true;document.body.classList.remove("has-card-info")}});
